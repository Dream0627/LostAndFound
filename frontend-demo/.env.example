# 复制为 .env（或 .env.local）后按需修改；Vite 只暴露以 VITE_ 开头的变量。
# 后端 API 基础地址：
#  - 留空或设为 /api/v1 时，走 vite.config.js 里的开发代理（推荐本地开发）。
#  - 前后端分离部署且已配置好 CORS 时，可改为完整地址，如 http://10.0.0.5:8080/api/v1
VITE_API_BASE_URL=/api/v1
